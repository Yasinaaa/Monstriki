<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="@dimen/margin_small">

    <ImageView
        android:layout_width="@dimen/size_prize_imageview"
        android:layout_height="@dimen/size_prize_imageview"
        android:id="@+id/iv_prize"
        android:background="@drawable/brain"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/tv_prze_title"
        android:text="@string/the_smartest"
        android:textSize="@dimen/textsize_prize_title"
        android:layout_marginTop="@dimen/margin_small"
        android:layout_centerHorizontal="true"
        android:textColor="@color/color_purple"
        android:textStyle="bold"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/tv_prze_date"
        android:text="30.10.2017"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/tv_prze_title"
        android:textColor="@color/color_purple"/>

</RelativeLayout>